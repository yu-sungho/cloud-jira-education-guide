# Jira 워크 아이템 만들기

## Create work items in Jira — Atlassian Community Learning 정리

> **출처**: [community.atlassian.com/learning/lesson/create-work-items-in-jira](https://community.atlassian.com/learning/lesson/create-work-items-in-jira)  
> **소요 시간**: 약 10분

---

## 학습 목표

이 레슨을 마치면 다음을 할 수 있습니다:

- Jira에서 **워크 아이템을 생성**할 수 있다
- 다양한 **작업 유형(Work type)**을 사용하여 업무를 구분할 수 있다
- **필드(Field)**를 활용하여 업무 정보를 기록할 수 있다

---

## 1. 작업 유형(Work type)이란?

다양한 업무를 체계적으로 분류하기 위해 Jira는 **작업 유형(Work type)** 을 사용합니다.  
작업 유형은 워크 아이템이 나타내는 업무의 **카테고리와 크기**를 나타냅니다.

> **예시**: `Subtask` 유형은 더 큰 산출물의 일부분임을 나타냅니다.

Jira는 업무의 크기와 카테고리를 기반으로 작업 유형을 **계층 구조**로 정리합니다.

### 작업 유형 계층 구조

```
Epic (에픽)
└── Story (스토리) / Task (태스크) / Bug (버그)
    └── Subtask (서브태스크)
```

### 주요 작업 유형 설명

| 유형        | 설명                                     | 크기      |
| ----------- | ---------------------------------------- | --------- |
| **Epic**    | 여러 스토리·태스크를 묶는 대형 업무 단위 | 가장 큼   |
| **Story**   | 사용자 관점의 기능 또는 요구사항         | 중간      |
| **Task**    | 완료해야 할 구체적인 작업                | 중간      |
| **Bug**     | 수정이 필요한 오류 또는 결함             | 중간      |
| **Subtask** | 상위 워크 아이템의 하위 세부 작업        | 가장 작음 |

### 작업 유형이 나타나는 위치

| 유형                 | 위치                                                       |
| -------------------- | ---------------------------------------------------------- |
| **Epic**             | 백로그(Backlog)의 Epics 패널. 팀 설정에 따라 보드에도 표시 |
| **Story, Task, Bug** | 보드 및 백로그                                             |
| **Subtask**          | 상위 워크 아이템 상세 페이지 내부                          |

에픽이 보이지 않는 경우 Epic 드롭다운에서 **Show epic panel**이 켜져 있는지 확인하세요.

![Jira 백로그 화면 - 에픽 목록이 강조 표시됨](https://dam-cdn.atl.orangelogic.com/AssetLink/26uy7cp24nxsl564d111m152c2g4s31d)

### 활용 예시 — 게임 디자인 팀

게임 디자인 팀이 우주를 배경으로 한 비디오 게임을 개발하는 상황:

```
Epic: 모든 캐릭터 프로필 개발 및 출시
├── Task: 핵심 캐릭터 V1 스케치
├── Task: 프로필 레이아웃 UI 디자인
├── Task: 데이터 시스템 구현
│   └── Subtask: 스테이징 환경 배포
└── Bug: 프로필 UI의 저장 버튼 오류 수정
```

---

## 2. 워크 아이템 생성하기

업무를 계획하고 정의하려면 먼저 워크 아이템을 생성해야 합니다.

### 생성 방법

- 메인 내비게이션 바의 **Create 버튼** 클릭
- 또는 키보드에서 **`C` 키** 누르기

### 상위 아이템 안에서 생성하기

상위 워크 아이템(예: 에픽, 태스크) 안에서 하위 워크 아이템을 생성할 수도 있습니다.

- 에픽 안에서 스토리 생성
- 태스크 안에서 서브태스크 생성

> **주의**: 서브태스크는 반드시 다른 워크 아이템 안에서만 생성할 수 있습니다.

서브태스크는 상위 워크 아이템 상세 페이지에서 확인할 수 있습니다.

![Jira 이슈 상세 페이지 - 서브태스크 목록이 강조 표시됨](https://dam-cdn.atl.orangelogic.com/AssetLink/yyh7hp3yu6nqh323v40xj4j32v123wh7)

---

## 3. 필드(Field)로 업무 정의하기

워크 아이템을 생성할 때 **필드(Field)** 를 사용하여 업무 정보를 기록합니다.  
필드는 워크 아이템에 대한 정보를 담는 항목입니다.

![워크 아이템의 필드 화면 - 각 필드가 강조 표시되고 각각을 가리키는 선이 표시됨](https://dam-cdn.atl.orangelogic.com/AssetLink/437phtmq31y1u25220hl37spp5d17cek.png)

### 주요 필드

| 필드              | 설명                                        | 편집 가능 여부 |
| ----------------- | ------------------------------------------- | -------------- |
| **Summary**       | 워크 아이템 제목/요약                       | 가능           |
| **Assignee**      | 담당자 (1명만 지정 가능)                    | 가능           |
| **Status**        | 현재 진행 상태 (To Do / In Progress / Done) | 가능           |
| **Priority**      | 우선순위 (Highest ~ Lowest)                 | 가능           |
| **Due Date**      | 마감일                                      | 가능           |
| **Label**         | 분류용 태그                                 | 가능           |
| **Work Item Key** | 워크 아이템 고유 식별자 (예: GDT-1)         | **불가**       |
| **Created**       | 생성 일시 (자동 생성)                       | **불가**       |
| **Updated**       | 마지막 수정 일시 (자동 업데이트)            | **불가**       |

> **참고**: 구체적인 필드는 팀의 스페이스 설정에 따라 달라집니다. 팀 리더에게 어떤 필드를 사용해야 하는지 확인하세요.

워크 아이템을 생성한 후에는 열어서 필드를 편집하고, 댓글을 추가하며, 업무에 필요한 모든 정보를 추적할 수 있습니다.

---

## 4. 워크 아이템 복제(Clone)하기

비슷한 워크 아이템을 여러 개 만들어야 할 때는 기존 워크 아이템을 **복제(Clone)** 할 수 있습니다.

> **예시**: 같은 담당자, 레이블, 마감일을 가진 워크 아이템이 3개 필요한 경우, 하나를 만들어 필드를 채운 뒤 두 번 복제하면 동일한 정보를 가진 3개의 워크 아이템을 빠르게 생성할 수 있습니다.

### 복제 방법

1. 복제할 워크 아이템을 엽니다.
2. 복제하려는 정보가 채워져 있는지 확인합니다.
3. 우측 상단의 **More actions** (•••)를 클릭합니다.
4. **Clone**을 선택합니다.

![Jira 이슈 화면 - "more actions" 메뉴에서 "clone"이 선택된 모습](https://dam-cdn.atl.orangelogic.com/AssetLink/ir1kwqu2y74v3184ewu7swa6c78w0dpn.png)

### 복제 시 처리 방식

| 항목                   | 처리 방식                                                   |
| ---------------------- | ----------------------------------------------------------- |
| **복제되는 정보**      | 작업 유형, 레이블, 담당자 등 대부분의 정보                  |
| **복제되지 않는 정보** | 댓글, 첨부 파일, 변경 이력 등                               |
| **자동 변경되는 정보** | 워크 아이템 키 (새로운 고유 키 자동 부여)                   |
| **복제 링크**          | 원본과 복제본 사이에 "Clones / is cloned by" 링크 자동 생성 |

---

## 핵심 개념 요약

```
워크 아이템 생성 흐름
├── Create 버튼 or C키 → 워크 아이템 생성 다이얼로그
├── 작업 유형 선택 → Epic / Story / Task / Bug / Subtask
├── 필드 입력 → Summary, Assignee, Priority, Due Date 등
└── 저장 → 고유 워크 아이템 키 자동 부여 (예: GDT-1)
```

| 개념                     | 한 줄 요약                                      |
| ------------------------ | ----------------------------------------------- |
| **작업 유형(Work type)** | 업무의 크기와 카테고리를 나타내는 분류          |
| **에픽(Epic)**           | 여러 하위 업무를 포함하는 대형 업무 묶음        |
| **서브태스크(Subtask)**  | 반드시 상위 워크 아이템 안에서만 생성 가능      |
| **필드(Field)**          | 워크 아이템의 정보를 담는 항목                  |
| **워크 아이템 키**       | 변경 불가한 고유 식별자 (예: GDT-1)             |
| **복제(Clone)**          | 기존 워크 아이템 정보를 복사하여 새 아이템 생성 |

---

_출처: [Atlassian Community Learning — Create work items in Jira](https://community.atlassian.com/learning/lesson/create-work-items-in-jira)_
